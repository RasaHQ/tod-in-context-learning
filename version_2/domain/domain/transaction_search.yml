version: "3.1"

intents:
  - transaction_search

actions:
  - transaction_search

slots:
  transactions_list:
    type: text
    mappings:
      - type: custom

responses:
  utter_transactions:
    - text: "Your current transactions are:\n {transactions_list}"
